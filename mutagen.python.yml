sync:
  defaults:
    mode: "one-way-replica"
    ignore:
      paths:
        - .idea
        - .vscode
        - venv
        - vendor
        - "*.egg-info"
        - dist
        - build
      vcs: true
